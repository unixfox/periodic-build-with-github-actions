name: Docker build litefs patched docker image

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/docker-build-litefs-patched.yml'
      - 'litefs-patched/**'

// ... existing code ... 