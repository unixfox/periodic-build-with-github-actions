name: Docker build opencode-web image

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/docker-build-opencode-web.yml'
      - 'opencode-web/**'

jobs:

  build:

    runs-on: 'ubuntu-latest'

    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: all

      - name: Check Out Repo
        uses: actions/checkout@v6

      - name: Login to Quay.io
        uses: docker/login-action@v3
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_PASSWORD }}

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v3
        with:
          version: latest

      - name: Set up build timestamp
        run: echo "timestamp=$(date +%Y%m%d)" >> $GITHUB_ENV

      - name: Build and push docker image
        id: docker_build_new
        uses: docker/build-push-action@v6
        with:
          context: ./opencode-web
          file: ./opencode-web/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: quay.io/unixfox/opencode-web:latest, quay.io/unixfox/opencode-web:build-${{ env.timestamp }}
