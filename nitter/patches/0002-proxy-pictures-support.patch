From 7fe5fafe364d3a2b9199be62a7360662a6caf332 Mon Sep 17 00:00:00 2001
From: Emilien <4016501+unixfox@users.noreply.github.com>
Date: Sat, 21 Mar 2026 12:36:12 +0100
Subject: [PATCH 2/4] proxy pictures support

---
 nitter.example.conf           |  1 +
 public/js/proxyneededcheck.js | 17 ++++++++++++++++
 src/parser.nim                |  2 +-
 src/prefs_impl.nim            |  3 +++
 src/routes/community.nim      |  4 ++--
 src/routes/list.nim           |  2 +-
 src/routes/router_utils.nim   |  2 +-
 src/routes/status.nim         |  2 +-
 src/routes/timeline.nim       |  2 +-
 src/utils.nim                 | 37 +++++++++++++++++++++++++----------
 src/views/about_account.nim   |  4 ++--
 src/views/article.nim         | 10 +++++-----
 src/views/broadcast.nim       |  4 ++--
 src/views/community.nim       | 10 +++++-----
 src/views/general.nim         | 11 +++++++----
 src/views/list.nim            |  6 +++---
 src/views/profile.nim         | 18 ++++++++---------
 src/views/renderutils.nim     | 12 ++++++------
 src/views/rss.nimf            | 14 ++++++-------
 src/views/space.nim           |  8 ++++----
 src/views/timeline.nim        |  2 +-
 src/views/tweet.nim           | 28 +++++++++++++-------------
 22 files changed, 120 insertions(+), 79 deletions(-)
 create mode 100644 public/js/proxyneededcheck.js

diff --git a/nitter.example.conf b/nitter.example.conf
index d9c1942..19ca7d8 100644
--- a/nitter.example.conf
+++ b/nitter.example.conf
@@ -45,5 +45,6 @@ replaceTwitter = "nitter.net"
 replaceYouTube = "piped.video"
 replaceReddit = "teddit.net"
 proxyVideos = true
+proxyPics = true
 hlsPlayback = false
 infiniteScroll = false
diff --git a/public/js/proxyneededcheck.js b/public/js/proxyneededcheck.js
new file mode 100644
index 0000000..8cc5010
--- /dev/null
+++ b/public/js/proxyneededcheck.js
@@ -0,0 +1,17 @@
+// This check if the browser can't load pbs.twimg.com domain.
+// If it can't then we force proxy on videos and pictures.
+if (!document.cookie.includes("proxyPics") && !document.cookie.includes("proxyVideos")) {
+    var img = new Image();
+    img.onerror = function () {
+        var expires = (new Date(Date.now() + 360 * 24 * 60 * 60 * 1000)).toUTCString();
+        if (location.protocol === 'https:') {
+            document.cookie = "proxyPics=on; path=/; Secure; expires=" + expires;
+            document.cookie = "proxyVideos=on; path=/; Secure; expires=" + expires;
+        } else {
+            document.cookie = "proxyPics=on; path=/; expires=" + expires;
+            document.cookie = "proxyVideos=on; path=/; expires=" + expires;
+        }
+        location.reload();
+    };
+    img.src = 'https://pbs.twimg.com/favicon.ico';
+}
diff --git a/src/parser.nim b/src/parser.nim
index 04a7d33..cc9c57f 100644
--- a/src/parser.nim
+++ b/src/parser.nim
@@ -460,7 +460,7 @@ proc parseCard(js: JsonNode; urls: JsonNode): Card =
 
   if kind in {promoImageConvo, promoImageApp, imageDirectMessage} and
      result.url.len == 0 or result.url.startsWith("card://"):
-    result.url = getPicUrl(result.image)
+    result.url = getPicUrl(result.image, true)
 
 proc parseTweet(js: JsonNode; jsCard: JsonNode = newJNull();
                 replyId: int64 = 0): Tweet =
diff --git a/src/prefs_impl.nim b/src/prefs_impl.nim
index 699ec4c..0d95fe9 100644
--- a/src/prefs_impl.nim
+++ b/src/prefs_impl.nim
@@ -94,6 +94,9 @@ genPrefs:
     proxyVideos(checkbox, true):
       "Proxy video streaming through the server (might be slow)"
 
+    proxyPics(checkbox, true):
+      "Proxy pictures through the server (might be slow)"
+
     muteVideos(checkbox, false):
       "Mute videos by default"
 
diff --git a/src/routes/community.nim b/src/routes/community.nim
index b850b6b..9d1ca30 100644
--- a/src/routes/community.nim
+++ b/src/routes/community.nim
@@ -13,7 +13,7 @@ template respCommunity*(cmty: Community; title: string; nav, vnode: typed) =
   if cmty.id.len == 0 or cmty.name.len == 0:
     resp Http404, showError(&"""Community "{@"id"}" not found""", cfg)
 
-  let html = renderCommunity(vnode, nav, cmty)
+  let html = renderCommunity(vnode, nav, cmty, prefs)
   resp renderMain(html, request, cfg, prefs, titleText=title, banner=cmty.banner)
 
 proc createCommunityRouter*(cfg: Config) =
@@ -56,7 +56,7 @@ proc createCommunityRouter*(cfg: Config) =
         mods = await getCachedCommunityModerators(cmty.id)
       respCommunity(cmty, cmty.name & " - About",
                      renderCommunityTabs(QueryKind.userList, cmty),
-                     renderCommunityAbout(cmty, mods))
+                     renderCommunityAbout(cmty, mods, prefs))
 
     get "/i/communities/@id/members":
       cond '.' notin @"id"
diff --git a/src/routes/list.nim b/src/routes/list.nim
index b4ab091..999ca35 100644
--- a/src/routes/list.nim
+++ b/src/routes/list.nim
@@ -12,7 +12,7 @@ template respList*(list, timeline, title, vnode: typed) =
     resp Http404, showError(&"""List "{@"id"}" not found""", cfg)
 
   let
-    html = renderList(vnode, timeline.query, list)
+    html = renderList(vnode, timeline.query, list, prefs.proxyPics)
     rss = if cfg.enableRSSList: &"""/i/lists/{@"id"}/rss""" else: ""
 
   resp renderMain(html, request, cfg, prefs, titleText=title, rss=rss, banner=list.banner)
diff --git a/src/routes/router_utils.nim b/src/routes/router_utils.nim
index 8a26193..c6d9e86 100644
--- a/src/routes/router_utils.nim
+++ b/src/routes/router_utils.nim
@@ -10,7 +10,7 @@ template savePref*(pref, value: string; req: Request; expire=false) =
   if not expire or pref in cookies(req):
     let sameSite = if cfg.useHttps: None else: Lax
     setCookie(pref, value, daysForward(when expire: -10 else: 360),
-              httpOnly=true, secure=cfg.useHttps, sameSite=sameSite, path="/")
+              httpOnly=false, secure=cfg.useHttps, sameSite=sameSite, path="/")
 
 template requestPrefs*(): untyped {.dirty.} =
   getPrefs(cookies(request), params(request))
diff --git a/src/routes/status.nim b/src/routes/status.nim
index f7fb1bb..1d512dd 100644
--- a/src/routes/status.nim
+++ b/src/routes/status.nim
@@ -56,7 +56,7 @@ proc createStatusRouter*(cfg: Config) =
         video = getVideoEmbed(cfg, conv.tweet.id)
       elif firstMediaKind == gifMedia:
         images = @[conv.tweet.media[0].getThumb]
-        video = getPicUrl(conv.tweet.media[0].gif.url)
+        video = getPicUrl(conv.tweet.media[0].gif.url, prefs.proxyPics)
       elif conv.tweet.card.isSome():
         let card = conv.tweet.card.get()
         if card.image.len > 0:
diff --git a/src/routes/timeline.nim b/src/routes/timeline.nim
index 6e8969e..e4591d1 100644
--- a/src/routes/timeline.nim
+++ b/src/routes/timeline.nim
@@ -131,7 +131,7 @@ proc createTimelineRouter*(cfg: Config) =
         resp showError(getSuspended(name), cfg)
       if info.username.len == 0:
         resp Http404, showError("User \"" & name & "\" not found", cfg)
-      let aboutHtml = renderAboutAccount(info)
+      let aboutHtml = renderAboutAccount(info, prefs.proxyPics)
       resp renderMain(aboutHtml, request, cfg, prefs,
                       "About @" & info.username)
 
diff --git a/src/utils.nim b/src/utils.nim
index 34e35db..87d80bb 100644
--- a/src/utils.nim
+++ b/src/utils.nim
@@ -48,21 +48,38 @@ proc getVidUrl*(link: string): string =
   else:
     &"{cdnUrl}/video/{sig}/{encodeUrl(link)}"
 
-proc getPicUrl*(link: string): string =
+proc getPicUrl*(link: string, proxyPics: bool): string =
   if link.len == 0: return
-  let sig = getHmac(link)
-  if base64Media:
-    &"{cdnUrl}/pic/enc/{sig}/{encode(link, safe=true)}"
+  if not proxyPics:
+    var url = link
+    if "twimg.com" notin url:
+      url.insert(twimg)
+    if not url.startsWith(https):
+      url.insert(https)
+    & "{url}"
   else:
-    &"{cdnUrl}/pic/{sig}/{encodeUrl(link)}"
+    let sig = getHmac(link)
+    if base64Media:
+      &"{cdnUrl}/pic/enc/{sig}/{encode(link, safe=true)}"
+    else:
+      &"{cdnUrl}/pic/{sig}/{encodeUrl(link)}"
 
-proc getOrigPicUrl*(link: string): string =
+proc getOrigPicUrl*(link: string, proxyPics: bool): string =
   if link.len == 0: return
-  let sig = getHmac(link)
-  if base64Media:
-    &"{cdnUrl}/pic/orig/enc/{sig}/{encode(link, safe=true)}"
+  if not proxyPics:
+    var url = link
+    if "twimg.com" notin url:
+      url.insert(twimg)
+    if not url.startsWith(https):
+      url.insert(https)
+    url.add("?name=orig")
+    & "{url}"
   else:
-    &"{cdnUrl}/pic/orig/{sig}/{encodeUrl(link)}"
+    let sig = getHmac(link)
+    if base64Media:
+      &"{cdnUrl}/pic/orig/enc/{sig}/{encode(link, safe=true)}"
+    else:
+      &"{cdnUrl}/pic/orig/{sig}/{encodeUrl(link)}"
 
 proc filterParams*(params: Table): seq[(string, string)] =
   for p in params.pairs():
diff --git a/src/views/about_account.nim b/src/views/about_account.nim
index aedd444..c4fe4c0 100644
--- a/src/views/about_account.nim
+++ b/src/views/about_account.nim
@@ -5,7 +5,7 @@ import karax/[karaxdsl, vdom]
 import renderutils
 import ".."/[types, formatters]
 
-proc renderAboutAccount*(info: AccountInfo): VNode =
+proc renderAboutAccount*(info: AccountInfo; proxyPics: bool): VNode =
   let user = User(
     username: info.username,
     fullname: info.fullname,
@@ -16,7 +16,7 @@ proc renderAboutAccount*(info: AccountInfo): VNode =
   buildHtml(tdiv(class="about-account")):
     tdiv(class="about-account-header"):
       a(class="about-account-avatar", href=(&"/{info.username}")):
-        genImg(getUserPic(info.userPic, "_200x200"))
+        genImg(getUserPic(info.userPic, "_200x200"), proxyPics)
       tdiv(class="about-account-name"):
         linkUser(user, class="profile-card-fullname")
         verifiedIcon(user)
diff --git a/src/views/article.nim b/src/views/article.nim
index eedd4cf..4465e99 100644
--- a/src/views/article.nim
+++ b/src/views/article.nim
@@ -31,8 +31,8 @@ proc renderAtomicParagraph(paragraph: ArticleParagraph; article: Article;
         of "ApiVideo":
           video(src=getVidUrl(media.url), controls="")
         else:
-          a(href=getOrigPicUrl(media.url), target="_blank"):
-            img(src=getSmallPic(media.url), alt=entity.caption, loading="lazy")
+          a(href=getOrigPicUrl(media.url, prefs.proxyPics), target="_blank"):
+            img(src=getSmallPic(media.url, prefs.proxyPics), alt=entity.caption, loading="lazy")
       if entity.caption.len > 0:
         p(class="article-media-caption"): text entity.caption
   of "TWEET":
@@ -198,7 +198,7 @@ proc renderArticle*(article: Article; tweets: Table[int64, Tweet];
     tdiv(class="article-author"):
       tdiv(class="article-author-row"):
         a(class="article-avatar", href=("/" & author.username)):
-          genImg(author.getUserPic("_bigger"), class=prefs.getAvatarClass)
+          genImg(author.getUserPic("_bigger"), prefs.proxyPics, class=prefs.getAvatarClass)
         tdiv(class="article-author-info"):
           tdiv(class="article-author-name"):
             linkUser(author, class="fullname")
@@ -242,7 +242,7 @@ proc renderArticle*(article: Article; tweets: Table[int64, Tweet];
 
   buildHtml(tdiv(class="article-page")):
     if article.coverImage.len > 0:
-      a(href=getOrigPicUrl(article.coverImage), target="_blank"):
-        img(class="article-cover", src=getSmallPic(article.coverImage), alt="")
+      a(href=getOrigPicUrl(article.coverImage, prefs.proxyPics), target="_blank"):
+        img(class="article-cover", src=getSmallPic(article.coverImage, prefs.proxyPics), alt="")
     main
     renderToTop()
diff --git a/src/views/broadcast.nim b/src/views/broadcast.nim
index bfcb9ba..4f752a5 100644
--- a/src/views/broadcast.nim
+++ b/src/views/broadcast.nim
@@ -8,7 +8,7 @@ import ".."/[types, utils, formatters]
 proc renderBroadcast*(bc: Broadcast; prefs: Prefs; path: string): VNode =
   let
     isLive = bc.state == "RUNNING"
-    thumb = getPicUrl(bc.thumb)
+    thumb = getPicUrl(bc.thumb, prefs.proxyPics)
     source = if prefs.proxyVideos and bc.m3u8Url.startsWith("http"):
                getVidUrl(bc.m3u8Url) else: bc.m3u8Url
     stateText =
@@ -59,7 +59,7 @@ proc renderBroadcast*(bc: Broadcast; prefs: Prefs; path: string): VNode =
 
         tdiv(class="broadcast-user-row"):
           a(class="broadcast-user", href=("/" & bc.user.username)):
-            genImg(getUserPic(bc.user.userPic, "_bigger"))
+            genImg(getUserPic(bc.user.userPic, "_bigger"), prefs.proxyPics)
             tdiv:
               tdiv:
                 strong: text bc.user.fullname
diff --git a/src/views/community.nim b/src/views/community.nim
index 52f9041..831ab58 100644
--- a/src/views/community.nim
+++ b/src/views/community.nim
@@ -39,7 +39,7 @@ proc renderHashtagHeader*(community: Community; tag: string): VNode =
   buildHtml(tdiv(class="community-hashtag-header")):
     h2(class="community-hashtag-title"): text "#" & tag
 
-proc renderCommunityAbout*(community: Community; moderators: seq[User]): VNode =
+proc renderCommunityAbout*(community: Community; moderators: seq[User]; prefs: Prefs): VNode =
   buildHtml(tdiv(class="community-about")):
     tdiv(class="community-info"):
       h2: text "Community Info"
@@ -91,7 +91,7 @@ proc renderCommunityAbout*(community: Community; moderators: seq[User]): VNode =
         for user in moderators:
           tdiv(class="community-moderator"):
             a(href=(&"/{user.username}")):
-              genImg(user.getUserPic("_bigger"), class="community-mod-avatar")
+              genImg(user.getUserPic("_bigger"), prefs.proxyPics, class="community-mod-avatar")
             tdiv(class="community-mod-info"):
               a(href=(&"/{user.username}"), class="community-mod-name"):
                 text user.fullname
@@ -100,12 +100,12 @@ proc renderCommunityAbout*(community: Community; moderators: seq[User]): VNode =
               a(href=(&"/{user.username}"), class="community-mod-username"):
                 text &"@{user.username}"
 
-proc renderCommunity*(body, nav: VNode; community: Community): VNode =
+proc renderCommunity*(body, nav: VNode; community: Community; prefs: Prefs): VNode =
   buildHtml(tdiv(class="timeline-container")):
     if community.banner.len > 0:
       tdiv(class="timeline-banner"):
-        a(href=getPicUrl(community.banner), target="_blank"):
-          genImg(community.banner)
+        a(href=getPicUrl(community.banner, prefs.proxyPics), target="_blank"):
+          genImg(community.banner, prefs.proxyPics)
 
     tdiv(class="community-header"):
       h1(class="community-name"):
diff --git a/src/views/general.nim b/src/views/general.nim
index 31ec329..37ffa66 100644
--- a/src/views/general.nim
+++ b/src/views/general.nim
@@ -70,6 +70,9 @@ proc renderHead*(prefs: Prefs; cfg: Config; req: Request; titleText=""; desc="";
     if rss.len > 0:
       link(rel="alternate", type="application/rss+xml", href=rss, title="RSS feed")
 
+    if not prefs.proxyPics and not prefs.proxyVideos:
+      script(src="/js/proxyneededcheck.js")
+
     if prefs.hlsPlayback:
       script(src="/js/hls.min.js", `defer`="")
       script(src="/js/hlsPlayback.js?v=1", `defer`="")
@@ -93,16 +96,16 @@ proc renderHead*(prefs: Prefs; cfg: Config; req: Request; titleText=""; desc="";
     meta(property="og:locale", content="en_US")
 
     if banner.len > 0 and not banner.startsWith('#'):
-      let bannerUrl = getPicUrl(banner)
+      let bannerUrl = getPicUrl(banner, prefs.proxyPics)
       link(rel="preload", type="image/png", href=bannerUrl, `as`="image")
 
     for url in images:
       if url.len == 0: continue
-      let preloadUrl = if "400x400" in url: getPicUrl(url)
-                       else: getSmallPic(url)
+      let preloadUrl = if "400x400" in url: getPicUrl(url, prefs.proxyPics)
+                       else: getSmallPic(url, prefs.proxyPics)
       link(rel="preload", type="image/png", href=preloadUrl, `as`="image")
 
-      let image = getPicUrl(url)
+      let image = getPicUrl(url, prefs.proxyPics)
       meta(property="og:image", content=image)
       meta(property="twitter:image:src", content=image)
 
diff --git a/src/views/list.nim b/src/views/list.nim
index e5639d1..bffbea2 100644
--- a/src/views/list.nim
+++ b/src/views/list.nim
@@ -12,12 +12,12 @@ proc renderListTabs*(query: Query; path: string): VNode =
     li(class=query.getTabClass(userList)):
       a(href=(path & "/members")): text "Members"
 
-proc renderList*(body: VNode; query: Query; list: List): VNode =
+proc renderList*(body: VNode; query: Query; list: List; proxyPics: bool): VNode =
   buildHtml(tdiv(class="timeline-container")):
     if list.banner.len > 0:
       tdiv(class="timeline-banner"):
-        a(href=getPicUrl(list.banner), target="_blank"):
-          genImg(list.banner)
+        a(href=getPicUrl(list.banner, proxyPics), target="_blank"):
+          genImg(list.banner, proxyPics)
 
     tdiv(class="timeline-header"):
       text &"\"{list.name}\" by @{list.username}"
diff --git a/src/views/profile.nim b/src/views/profile.nim
index de09368..101a41f 100644
--- a/src/views/profile.nim
+++ b/src/views/profile.nim
@@ -23,13 +23,13 @@ proc renderUserCard*(user: User; prefs: Prefs; info: AccountInfo): VNode =
   buildHtml(tdiv(class="profile-card")):
     tdiv(class="profile-card-info"):
       let
-        url = getPicUrl(user.getUserPic())
+        url = getPicUrl(user.getUserPic(), prefs.proxyPics)
         size =
           if prefs.autoplayGifs and user.userPic.endsWith("gif"): ""
           else: "_400x400"
 
       a(class="profile-card-avatar", href=url, target="_blank"):
-        genImg(user.getUserPic(size))
+        genImg(user.getUserPic(size), prefs.proxyPics)
 
       tdiv(class="profile-card-tabs-name"):
         linkUser(user, class="profile-card-fullname")
@@ -76,7 +76,7 @@ proc renderUserCard*(user: User; prefs: Prefs; info: AccountInfo): VNode =
           renderStatLink(user.followers, "followers", &"/{user.username}/followers")
           renderStat(user.likes, "likes")
 
-proc renderPhotoRail(profile: Profile): VNode =
+proc renderPhotoRail(profile: Profile; proxyPics: bool): VNode =
   let count = insertSep($profile.user.media, ',')
   buildHtml(tdiv(class="photo-rail-card")):
     tdiv(class="photo-rail-header"):
@@ -95,16 +95,16 @@ proc renderPhotoRail(profile: Profile): VNode =
           if "format" in photo.url or "placeholder" in photo.url: ""
           else: ":thumb"
         a(href=(&"/{profile.user.username}/status/{photo.tweetId}#m")):
-          genImg(photo.url & photoSuffix)
+          genImg(photo.url & photoSuffix, proxyPics)
 
-proc renderBanner(banner: string): VNode =
+proc renderBanner(banner: string; proxyPics: bool): VNode =
   buildHtml():
     if banner.len == 0:
       a()
     elif banner.startsWith('#'):
       a(style={backgroundColor: banner})
     else:
-      a(href=getPicUrl(banner), target="_blank"): genImg(banner)
+      a(href=getPicUrl(banner, proxyPics), target="_blank"): genImg(banner, proxyPics)
 
 proc renderProtected*(username: string): VNode =
   buildHtml(tdiv(class="timeline-container")):
@@ -122,14 +122,14 @@ proc renderProfile*(profile: var Profile; prefs: Prefs; path: string): VNode =
   buildHtml(tdiv(class=("profile-tabs" & viewClass))):
     if not isGalleryView and not prefs.hideBanner:
       tdiv(class="profile-banner"):
-        renderBanner(profile.user.banner)
+        renderBanner(profile.user.banner, prefs.proxyPics)
 
     if not isGalleryView:
       let sticky = if prefs.stickyProfile: " sticky" else: ""
       tdiv(class=("profile-tab" & sticky)):
         renderUserCard(profile.user, prefs, profile.accountInfo)
         if profile.photoRail.len > 0:
-          renderPhotoRail(profile)
+          renderPhotoRail(profile, prefs.proxyPics)
 
     if profile.user.protected:
       renderProtected(profile.user.username)
@@ -154,7 +154,7 @@ proc renderUserList*(user: User; results: Result[User]; prefs: Prefs;
   buildHtml(tdiv(class="profile-tabs")):
     if not prefs.hideBanner:
       tdiv(class="profile-banner"):
-        renderBanner(user.banner)
+        renderBanner(user.banner, prefs.proxyPics)
 
     let sticky = if prefs.stickyProfile: " sticky" else: ""
     tdiv(class=("profile-tab" & sticky)):
diff --git a/src/views/renderutils.nim b/src/views/renderutils.nim
index af2f05d..8afae08 100644
--- a/src/views/renderutils.nim
+++ b/src/views/renderutils.nim
@@ -6,19 +6,19 @@ import ".."/[types, utils]
 const smallWebp* = "?name=small&format=webp"
 const mediumWebp* = "?name=medium&format=webp"
 
-proc getSmallPic*(url: string): string =
+proc getSmallPic*(url: string; proxyPics: bool): string =
   if url.len == 0: return
   result = url
   if "?" notin url and not url.endsWith("placeholder.png"):
     result &= smallWebp
-  result = getPicUrl(result)
+  result = getPicUrl(result, proxyPics)
 
-proc getMediumPic*(url: string): string =
+proc getMediumPic*(url: string; proxyPics: bool): string =
   if url.len == 0: return
   result = url
   if "?" notin url and not url.endsWith("placeholder.png"):
     result &= mediumWebp
-  result = getPicUrl(result)
+  result = getPicUrl(result, proxyPics)
 
 proc icon*(icon: string; label=""; title=""; class=""; href=""): VNode =
   var c = "icon-" & icon
@@ -106,9 +106,9 @@ proc genNumberInput*(pref, label, state, placeholder: string; class=""; autofocu
       label(`for`=pref): text label
     input(name=pref, `type`="number", placeholder=p, value=state, autofocus=(autofocus and state.len == 0), min=min, step="1")
 
-proc genImg*(url: string; class=""; alt=""): VNode =
+proc genImg*(url: string; proxyPics: bool; class=""; alt=""): VNode =
   buildHtml():
-    img(src=getPicUrl(url), class=class, alt=alt, loading="lazy")
+    img(src=getPicUrl(url, proxyPics), class=class, alt=alt, loading="lazy")
 
 proc getTabClass*(query: Query; tab: QueryKind): string =
   if query.kind == tab: "tab-item active"
diff --git a/src/views/rss.nimf b/src/views/rss.nimf
index 0cbeccf..776d857 100644
--- a/src/views/rss.nimf
+++ b/src/views/rss.nimf
@@ -56,17 +56,17 @@ Twitter feed for: ${desc}. Generated by ${getUrlPrefix(cfg)}
 #case media.kind
 #of photoMedia:
 #  let photo = media.photo
-<img src="${getPicUrl(photo.url)}" style="max-width:250px;" />
+<img src="${getPicUrl(photo.url, false)}" style="max-width:250px;" />
 #of videoMedia:
 #  let video = media.video
 <a href="${urlPrefix}${tweet.getLink}">
 <br>Video<br>
-  <img src="${getPicUrl(video.thumb)}" style="max-width:250px;" />
+  <img src="${getPicUrl(video.thumb, false)}" style="max-width:250px;" />
 </a>
 #of gifMedia:
 #  let gif = media.gif
-#  let thumb = &"{getPicUrl(gif.thumb)}"
-#  let url = &"{getPicUrl(gif.url)}"
+#  let thumb = &"{getPicUrl(gif.thumb, false)}"
+#  let url = &"{getPicUrl(gif.url, false)}"
 <video poster="${thumb}" autoplay muted loop style="max-width:250px;">
   <source src="${url}" type="video/mp4"></video>
 #end case
@@ -81,7 +81,7 @@ Twitter feed for: ${desc}. Generated by ${getUrlPrefix(cfg)}
 <a href="${xmltree.escape(cardLink)}">
 #end if
 #if card.image.len > 0:
-<img src="${getPicUrl(card.image)}" style="max-width:250px;" />
+<img src="${getPicUrl(card.image, false)}" style="max-width:250px;" />
 #end if
 #if title.len > 0:
 #  if card.image.len > 0:
@@ -109,7 +109,7 @@ Twitter feed for: ${desc}. Generated by ${getUrlPrefix(cfg)}
 <b>Article</b><br>
 <a href="${link}">
 #if article.coverImage.len > 0:
-<img src="${getPicUrl(article.coverImage)}" style="max-width:250px;" />
+<img src="${getPicUrl(article.coverImage, false)}" style="max-width:250px;" />
 #end if
 #if article.title.len > 0:
 #  if article.coverImage.len > 0:
@@ -246,7 +246,7 @@ ${renderRssTweet(quoteTweet, cfg, prefs)}
     <image>
       <title>${title}</title>
       <link>${urlPrefix}/${profile.user.username}</link>
-      <url>${getPicUrl(profile.user.getUserPic(style="_400x400"))}</url>
+      <url>${getPicUrl(profile.user.getUserPic(style="_400x400"), false)}</url>
       <width>128</width>
       <height>128</height>
     </image>
diff --git a/src/views/space.nim b/src/views/space.nim
index a5cac7b..6aa77f4 100644
--- a/src/views/space.nim
+++ b/src/views/space.nim
@@ -5,10 +5,10 @@ import karax/[karaxdsl, vdom]
 import renderutils
 import ".."/[types, utils, formatters]
 
-proc renderParticipant(p: SpaceParticipant; role: string): VNode =
+proc renderParticipant(p: SpaceParticipant; role: string; prefs: Prefs): VNode =
   buildHtml(tdiv(class="space-participant")):
     a(href=("/" & p.username)):
-      genImg(p.avatarUrl.replace("_normal", "_bigger"))
+      genImg(p.avatarUrl.replace("_normal", "_bigger"), prefs.proxyPics)
       tdiv(class="participant-info"):
         tdiv(class="participant-name"):
           strong: text p.displayName
@@ -81,6 +81,6 @@ proc renderSpace*(sp: AudioSpace; prefs: Prefs; path: string): VNode =
             for admin in sp.admins:
               let role = if admin.username == sp.creator.username: "Host"
                          else: "Co-host"
-              renderParticipant(admin, role)
+              renderParticipant(admin, role, prefs)
             for speaker in sp.speakers:
-              renderParticipant(speaker, "")
+              renderParticipant(speaker, "", prefs)
diff --git a/src/views/timeline.nim b/src/views/timeline.nim
index 9456bf9..1edb260 100644
--- a/src/views/timeline.nim
+++ b/src/views/timeline.nim
@@ -87,7 +87,7 @@ proc renderUser(user: User; prefs: Prefs): VNode =
     tdiv(class="tweet-body profile-result"):
       tdiv(class="tweet-header"):
         a(class="tweet-avatar", href=("/" & user.username)):
-          genImg(user.getUserPic("_bigger"), class=prefs.getAvatarClass)
+          genImg(user.getUserPic("_bigger"), prefs.proxyPics, class=prefs.getAvatarClass)
 
         tdiv(class="tweet-name-row"):
           tdiv(class="fullname-and-username"):
diff --git a/src/views/tweet.nim b/src/views/tweet.nim
index b66269a..1e64f75 100644
--- a/src/views/tweet.nim
+++ b/src/views/tweet.nim
@@ -10,7 +10,7 @@ import general
 const doctype = "<!DOCTYPE html>\n"
 
 proc renderMiniAvatar*(user: User; prefs: Prefs): VNode =
-  genImg(user.getUserPic("_mini"), class=(prefs.getAvatarClass & " mini"))
+  genImg(user.getUserPic("_mini"), prefs.proxyPics, class=(prefs.getAvatarClass & " mini"))
 
 proc renderArticleCard(preview: ArticlePreview; prefs: Prefs): VNode =
   let url = "/i/article/" & $preview.tweetId
@@ -19,7 +19,7 @@ proc renderArticleCard(preview: ArticlePreview; prefs: Prefs): VNode =
       if preview.coverImage.len > 0:
         tdiv(class="card-image-container"):
           tdiv(class="card-image"):
-            genImg(preview.coverImage)
+            genImg(preview.coverImage, prefs.proxyPics)
           span(class="article-card-badge"): text "Article"
       tdiv(class="card-content-container"):
         tdiv(class="card-content"):
@@ -45,7 +45,7 @@ proc renderHeader(tweet: Tweet; retweet: string; pinned: bool; prefs: Prefs;
         var size = "_bigger"
         if not prefs.autoplayGifs and tweet.user.userPic.endsWith("gif"):
           size = "_400x400"
-        genImg(tweet.user.getUserPic(size), class=prefs.getAvatarClass)
+        genImg(tweet.user.getUserPic(size), prefs.proxyPics, class=prefs.getAvatarClass)
 
       tdiv(class="tweet-name-row"):
         tdiv(class="fullname-and-username"):
@@ -61,15 +61,15 @@ proc renderAltText(altText: string): VNode =
   buildHtml(p(class="alt-text")):
     text "ALT  " & altText
 
-proc renderPhotoAttachment(photo: Photo; bigThumb=false): VNode =
+proc renderPhotoAttachment(photo: Photo; proxyPics: bool; bigThumb=false): VNode =
   buildHtml(tdiv(class="attachment")):
     let
       named = "name=" in photo.url
       thumb = if named: photo.url
               elif bigThumb: photo.url & mediumWebp
               else: photo.url & smallWebp
-    a(href=getOrigPicUrl(photo.url), class="still-image", target="_blank"):
-      genImg(thumb, alt=photo.altText)
+    a(href=getOrigPicUrl(photo.url, proxyPics), class="still-image", target="_blank"):
+      genImg(thumb, proxyPics, alt=photo.altText)
     if photo.altText.len > 0:
       renderAltText(photo.altText)
 
@@ -107,7 +107,7 @@ proc renderVideoAttachment(videoData: Video; prefs: Prefs; path=""; bigThumb=fal
   let
     playbackType = if not prefs.proxyVideos and videoData.hasMp4Url: mp4
                    else: videoData.playbackType
-    thumb = if bigThumb: getMediumPic(videoData.thumb) else: getSmallPic(videoData.thumb)
+    thumb = if bigThumb: getMediumPic(videoData.thumb, prefs.proxyPics) else: getSmallPic(videoData.thumb, prefs.proxyPics)
 
   buildHtml(tdiv(class="attachment")):
     if not videoData.available:
@@ -152,7 +152,7 @@ proc renderVideo*(video: Video; prefs: Prefs; path: string; bigThumb=false): VNo
             p(class="card-description"): text video.description
 
 proc renderGifAttachment(gif: Gif; prefs: Prefs; path=""): VNode =
-  let thumb = getSmallPic(gif.thumb)
+  let thumb = getSmallPic(gif.thumb, prefs.proxyPics)
 
   buildHtml(tdiv(class="attachment")):
     if not prefs.mp4Playback:
@@ -160,10 +160,10 @@ proc renderGifAttachment(gif: Gif; prefs: Prefs; path=""): VNode =
       renderVideoDisabled(mp4, path)
     elif prefs.autoplayGifs:
       video(class="gif", poster=thumb, autoplay="", muted="", loop=""):
-        source(src=getPicUrl(gif.url), `type`="video/mp4")
+        source(src=getPicUrl(gif.url, prefs.proxyPics), `type`="video/mp4")
     else:
       video(class="gif", poster=thumb, controls="", muted="", loop=""):
-        source(src=getPicUrl(gif.url), `type`="video/mp4")
+        source(src=getPicUrl(gif.url, prefs.proxyPics), `type`="video/mp4")
     if gif.altText.len > 0:
       renderAltText(gif.altText)
 
@@ -195,7 +195,7 @@ proc renderMedia(media: seq[Media]; prefs: Prefs; path: string; bigThumb=false):
         for mediaItem in mediaGroup:
           case mediaItem.kind
           of photoMedia:
-            renderPhotoAttachment(mediaItem.photo, bigThumb)
+            renderPhotoAttachment(mediaItem.photo, prefs.proxyPics, bigThumb)
           of videoMedia:
             renderVideoAttachment(mediaItem.video, prefs, path, bigThumb)
           of gifMedia:
@@ -216,10 +216,10 @@ proc renderPoll(poll: Poll): VNode =
     span(class="poll-info"):
       text &"{insertSep($poll.votes, ',')} votes • {poll.status}"
 
-proc renderCardImage(card: Card): VNode =
+proc renderCardImage(card: Card; proxyPics: bool): VNode =
   buildHtml(tdiv(class="card-image-container")):
     tdiv(class="card-image"):
-      genImg(card.image)
+      genImg(card.image, proxyPics)
       if card.kind == player:
         tdiv(class="card-overlay"):
           tdiv(class="overlay-circle"):
@@ -247,7 +247,7 @@ proc renderCard(card: Card; prefs: Prefs; path: string): VNode =
     else:
       a(class="card-container", href=url):
         if card.image.len > 0:
-          renderCardImage(card)
+          renderCardImage(card, prefs.proxyPics)
         tdiv(class="card-content-container"):
           renderCardContent(card)
 
-- 
2.54.0

